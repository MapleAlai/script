
rootpath="./function"
for name in `ls $rootpath`;do
    if [ ! $name = "all_function" ];then
        source $rootpath/$name
        __LIST__=$__LIST__$name"\t\t`$name"_readme"`\n"
    fi
done

function_list(){
    echo -en $__LIST__
}